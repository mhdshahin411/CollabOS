# Copy to n8n/.env and fill in.  cp .env.example .env
POSTGRES_PASSWORD=change-me-to-a-long-random-string
# Generate: openssl rand -hex 32   — then NEVER change it (encrypts saved credentials)
N8N_ENCRYPTION_KEY=paste-openssl-rand-hex-32-output-here
